# Expo / React Native + Metro work most reliably with a hoisted node_modules
# layout in a pnpm monorepo (avoids symlink resolution issues in Metro).
node-linker=hoisted
public-hoist-pattern[]=*react*
public-hoist-pattern[]=*react-dom*
public-hoist-pattern[]=*scheduler*
